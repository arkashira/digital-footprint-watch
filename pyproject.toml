[project]
name = "digital-footprint-watch"
version = "0.1.0"
description = "A simple crawler to discover public mentions of a developer's email."
readme = "README.md"
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = ["tests"]
