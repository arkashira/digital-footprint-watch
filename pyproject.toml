[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "digital-footprint-watch"
version = "0.1.0"
description = "A minimal platform for digital footprint management."
requires-python = ">=3.8"

[tool.pytest.ini_options]
addopts = "-q"
